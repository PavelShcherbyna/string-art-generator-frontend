import styled from 'styled-components';

export const AnotherInputContainer = styled.label`
  cursor: pointer;
  padding: 12px 40px;
  font-size: 18px;
  font-style: normal;
  font-weight: 600;
  line-height: 22px;
  color: #000000;

  img {
    margin-right: 18px;
  }
`;
